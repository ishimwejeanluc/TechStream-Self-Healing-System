# Root cause analysis: TechStream incident

Generated 2026-08-17T13:39:24Z by `rca/rca.py`.

> **This is a lab.** Every request in this window was generated by
> `chaos/chaos.sh`. No real users were affected and no production system
> was involved. The fault was injected deliberately.

## Verdict

**The error ratio breached first, consistent with an application fault rather than resource exhaustion.**

- Verdict: `application_fault`
- Confidence: high
- Recommended remediation: `restart`

Error ratio crossed 5 percent first at 2026-08-17T13:30:42Z, peaking at 41.7877 percent, which is 8.358 times the alert threshold. No saturation signal breached at any point in the window, so the host and container had resources to spare while the application was returning errors. Restarting the process is the correct remediation for a fault that lives in process state, and that is what the automation does.

## Incident window

| Field | Value |
|---|---|
| Chaos mode | `errors` |
| Injected error rate | 0.4 |
| Injected CPU cores | None |
| Started | 2026-08-17T13:30:12Z |
| Injection ended | 2026-08-17T13:32:12Z |
| Run ended | 2026-08-17T13:36:12Z |
| Duration | 360s |
| Requests sent | 6960 |
| 5xx responses | 771 |
| Observed rate | 19 req/s |

## Signal timeline

Ordered by when each signal first breached. A breach has to hold for 2 consecutive samples (30s) before it counts, so a single noisy scrape is not treated as an incident.

| Signal | Baseline | Peak | Threshold | Peak vs threshold | First breach | Lead | Recovered |
|---|---|---|---|---|---|---|---|
| Error ratio | n/a | 41.788 % | 5 % | 8.358x | 2026-08-17T13:30:42Z | first | - |
| p95 latency | n/a | 0.222 s | 0.2 s | 1.11x | 2026-08-17T13:30:57Z | +15s | 2026-08-17T13:31:42Z |
| Host CPU busy | 1.459 % | 15.828 % | 85 % | 0.186x | not breached | - | - |
| App container CPU | 0.005 cores | 0.312 cores | 1 cores | 0.312x | not breached | - | - |
| Request rate | 0 req/s | 20 req/s | none | - | not breached | - | - |

## Ranking

**Which moved first**

1. Error ratio at 2026-08-17T13:30:42Z
2. p95 latency at 2026-08-17T13:30:57Z, 15s later

**Which deviated most**

1. Error ratio peaked at 41.788 %, 8.358x its threshold of 5
2. p95 latency peaked at 0.222 s, 1.11x its threshold of 0.2

## Remediation correlation

- Restarts detected during the run: **1**
- Error ratio recovered at: not observed directly, see the cessation evidence below
- Time from first breach to recovery: n/a

The app container restarted 1 time(s) during the window. The 5xx series stops after 2026-08-17T13:32:12Z, while traffic kept flowing for a further 285s at a mean of 16.82 req/s across 19 samples. Requests were still being served and none of them failed, so errors genuinely ceased rather than measurement stopping. Consistent with automated remediation resolving the incident.

*Caveat.* Recovery here is inferred rather than read directly. After a restart the app is a fresh process with counters at zero, so the 5xx series stops existing rather than reporting zero, and the error ratio expression returns no data. The inference above is only sound because traffic continued through the gap, which rules out the alternative explanation that measurement simply stopped.

### How cessation was established

- Errors ceased at: **2026-08-17T13:32:12Z**
- From first breach to cessation: 90s
- Traffic samples after the last error: 19 at a mean of 16.82 req/s

The 5xx series stops after 2026-08-17T13:32:12Z, while traffic kept flowing for a further 285s at a mean of 16.82 req/s across 19 samples. Requests were still being served and none of them failed, so errors genuinely ceased rather than measurement stopping.

### Signals without a baseline

No pre-injection samples for: Error ratio, p95 latency.

Signals with no pre-injection samples have no baseline. This is normal for traffic derived signals when the system was idle before the run: no requests means no series to measure.

## How this analysis works

No machine learning is involved. The steps are:

1. Read the incident window from `chaos/incident.json`, including a 300 second baseline before injection started.
2. Query each signal over that window with `/api/v1/query_range` at a 15s step.
3. Take the median of the pre-injection samples as the baseline. Median rather than mean, so one spike does not move it.
4. Find the first run of 2 consecutive samples above the threshold, and the first sustained run back below it.
5. Rank by first breach time and by peak divided by threshold.
6. Decide the verdict from which class of signal moved first: saturation first reads as resource exhaustion, application error first reads as an application fault.

The reasoning is a documented rule set rather than a model, which means the conclusion can be checked against the table above by hand.

## Caveats

- Traffic is synthetic, generated by `chaos.sh`. Absolute request rates mean nothing outside this lab.
- The error ratio uses `clamp_min(..., 1)` on the denominator. Below 1 request per second it understates the true ratio, so the number is only trustworthy while traffic is flowing.
- `histogram_quantile` returns NaN when a window has no requests. Those samples are dropped, not read as zero, so latency gaps appear as missing data rather than as fast responses.
- Host CPU comes from node_exporter, which reads the kernel it runs on. Under Docker Desktop or OrbStack on a Mac that is the Linux VM, not macOS.
- Thresholds for error ratio, latency and host CPU match the alert rules. The container CPU threshold of 1 core is a lab heuristic with no corresponding alert.


